import { EmailValidDirective } from './email-valid.directive';

describe('EmailValidDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailValidDirective();
    expect(directive).toBeTruthy();
  });
});
